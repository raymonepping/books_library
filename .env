# .env
VERSION=v1.1.0
DOCKERHUB_REPO=repping
LOG_DIR=./logs

# Optional for validation script extras
DEBUG=true                 # Optional: enables more verbose logging
DRY_RUN=false              # Optional: skip actual signing, just check config

# Versions
CB_VERSION=enterprise-7.6.3
VAULT_VERSION=1.16
MINIO_VERSION=latest

# Vault connection
VAULT_ADDR=http://127.0.0.1:8200

# Ports
API_PORT=3001
WEB_PORT=3000
VAULT_PORT=8200
MINIO_PORT=9000
MINIO_CONSOLE_PORT=9001

# Credentials (bootstrap only – will move into Vault later)
CB_ADMIN_USER=Administrator
CB_ADMIN_PASS=password
MINIO_ROOT_USER=minio
MINIO_ROOT_PASSWORD=minio12345

# App
NODE_ENV=development
